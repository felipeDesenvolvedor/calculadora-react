import React from "react"
import styled from "styled-components"

const Button = styled.button`
    margin:1px;
    color:white;
    background-color:var(--color-ButtonsGrupoUm);
    border:none;
    cursor:pointer;
`

export default Button